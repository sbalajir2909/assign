import ChatPage from '@/components/ChatPage'
export default function Spark() {
  return <ChatPage mode="spark" emoji="⚡" accentColor="#FFE500" initialMessage="yo, what's the one thing you're stuck on right now?" placeholder="what are you stuck on?" tagline="quick session" />
}
